print("Installed packages (before activation of renv): ")
print(rownames(installed.packages()))

print("Activating {renv}")
source("renv/activate.R")

print("Installed packages (after activation of renv): ")
print(rownames(installed.packages()))
